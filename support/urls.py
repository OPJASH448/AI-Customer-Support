from django.urls import path
from . import views

app_name = 'support'

urlpatterns = [
    # Add your support app URLs here
    # path('documents/', views.document_list, name='document_list'),
    # path('conversations/', views.conversation_list, name='conversation_list'),
]
